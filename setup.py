#!/usr/bin/env python
"""Setup script for backward compatibility."""
from setuptools import setup

# This setup.py is kept only for backward compatibility
# Please use pip install -e . with pyproject.toml for local development
# or build with python -m build

if __name__ == "__main__":
    setup()
